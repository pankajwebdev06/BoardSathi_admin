// M5 placeholder — review queue lands after AI generation (M2) produces
// draft questions. Kept as a stub so navigation and routing are stable.
export default function ReviewQueuePage() {
  return (
    <div>
      <h2 className="mb-2 text-2xl font-bold text-gray-900">Review Queue</h2>
      <p className="text-sm text-gray-500">
        Draft questions will appear here for review → approve → publish (module
        M5). Build this after AI question generation (M2) produces drafts.
      </p>
    </div>
  );
}
